{
  "name": "PublicJobAccess",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "description": "Linked Job record id"
    },
    "job_id": {
      "type": "string",
      "description": "Linked Job record id, snake_case compatibility"
    },
    "tokenHash": {
      "type": "string",
      "description": "SHA-256 hash of the raw public access token. Raw tokens are never stored."
    },
    "token_hash": {
      "type": "string",
      "description": "SHA-256 hash of the raw public access token, snake_case compatibility"
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time",
      "description": "Optional expiry date for this public token"
    },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "revokedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Set when this public token is revoked"
    },
    "revoked_at": {
      "type": "string",
      "format": "date-time"
    },
    "permissions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [
        "view_status",
        "view_booking",
        "add_note",
        "upload_file"
      ],
      "description": "Allowed public actions for this token"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "description": {
      "type": "string",
      "maxLength": 1000
    }
  },
  "required": [
    "jobId",
    "tokenHash"
  ],
  "rls": {
    "create": true,
    "read": false,
    "update": true,
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}